# Development Environment Configuration
# This file contains settings for local development with H2 database

# Spring Profile
SPRING_PROFILES_ACTIVE=dev

# JWT Configuration (dev keys - NOT for production)
JWT_SECRET=dev-secret-key-for-local-development-only-not-for-production-change-this
JWT_EXPIRATION=86400000

# Container Configuration
BACKEND_PORT=8080

# Note: Development profile uses H2 in-memory database
# No PostgreSQL configuration needed for dev
